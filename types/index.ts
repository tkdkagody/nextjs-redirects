export interface SeoProps {
  title: string;
  img: string;
}

export interface StageInfo {
  _id: string;
  title: string;
  thumbnailUrl: string;
}

export interface StageDetail {
  title: string;
  thumbnailUrl: string;
}
